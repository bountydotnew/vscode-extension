{
  "name": "bounty-extension",
  "displayName": "Bounty",
  "description": "Bounty VSCode Extension",
  "version": "0.0.1",
  "publisher": "bountydotnew",
  "private": true,
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Other"
  ],
  "main": "./dist/extension.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/bountydotnew/vscode-extension.git"
  },
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "bounty-sidebar",
          "title": "Bounty",
          "icon": "resources/icon.svg"
        }
      ]
    },
    "views": {
      "bounty-sidebar": [
        {
          "type": "webview",
          "id": "bounty.sidebarView",
          "name": "Bounty",
          "icon": "resources/icon.svg"
        }
      ]
    },
    "commands": [
      {
        "command": "bounty-vscode.helloWorld",
        "title": "Hello World"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run webview:build && npm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "watch:webview": "cd webview && npm start",
    "package": "npm run webview:build && npm run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test",
    "webview:install": "node ./scripts/install-webview.js",
    "webview:build": "cd webview && npm run build",
    "webview:dev": "cd webview && npm start",
    "webview:clean": "cd webview && npm run clean",
    "vscode:build": "npx @vscode/vsce package --no-dependencies",
    "postinstall": "node ./scripts/install-webview.js"
  },
  "dependencies": {
    "@trpc/client": "^11.4.3",
    "superjson": "^2.2.2"
  },
  "devDependencies": {
    "@types/vscode": "^1.90.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "^24.1.0",
    "@typescript-eslint/eslint-plugin": "^8.45.0",
    "@typescript-eslint/parser": "^8.45.0",
    "eslint": "^9.29.0",
    "esbuild": "^0.25.10",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.6.3",
    "@vscode/test-cli": "^0.0.11",
    "@vscode/test-electron": "^2.5.2"
  }
}
